# Core dependencies for Spiking RNN Heterogeneity Framework v3.5.0
# Split experiments architecture: spontaneous activity + network stability analysis
# NEW: Poisson process analysis, rate-dependent RNG, transient removal, fixed time-step bug

# Numerical computing
numpy>=1.20.0
scipy>=1.7.0

# Parallel processing for split MPI experiments
mpi4py>=3.1.0

# System monitoring and CPU load balancing
psutil>=5.8.0

# Visualization (optional but recommended for Poisson analysis plots)
matplotlib>=3.5.0

# Statistical analysis for Poisson process validation
# (Uses scipy.stats - no additional dependency needed)

# Random number generation for rate-dependent seeds and job scrambling
# (Uses built-in random module - no additional dependency needed)